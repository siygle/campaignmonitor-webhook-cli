'use strict'

const request = require('request')

function getListWebhook () {
  return new Promise((resolve, reject) => {

  })
}

module.exports = {
  handler: function (action, argv) {
    switch (action) {
      case 'GET':
        break
      default:
    }
  }
}
